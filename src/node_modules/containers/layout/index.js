import React from 'react'

const Layout = ({children}) => (
    <div className="container-fluid">
	    <header>
	    		Header
	    </header>
 
        {children}
    <footer>
    		Footer
    </footer>

    </div>
)

export default Layout
