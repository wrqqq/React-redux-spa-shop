import React, {Component} from 'react'
import {connect} from 'react-redux'
import {getRobotById} from 'selectors'
import {Link} from 'react-router'

import {fetchRobotById} from 'actions'

class Robot extends Component {
    componentDidMount() {
        this.props.fetchRobotById(this.props.params.id)
    }
    renderPage() {
        const {robot} = this.props
        console.log(robot)
        return (
            <div>
                <div className="col-md-6 col-sm-12 col-xs-12">
                    <img src={robot.image} className="single-image" alt={robot.name}/>
                </div>
                <div className="col-md-6 col-sm-12 col-xs-12">
                    <h3 className='robotName'>{robot.name}</h3>
                    <p className="description">{robot.description}</p>
                    <span className="single-price">{robot.price} $</span>
                    <Link to={`/`} className="back">
                        Back
                    </Link>
                </div>
            </div>
        )
    }
    render() {
        const {robot} = this.props
        console.log('robot', this.props.robot)
        return (
            <div className="container">
                <div className="row">
                    {robot && this.renderPage()}
                </div>
            </div>
        )
    }
}

const mapStateToProps = state => ({
    robot: getRobotById(state, state.robotPage.id)
})

const mapDispatchToProps = {
    fetchRobotById
}

export default connect(mapStateToProps, mapDispatchToProps)(Robot)