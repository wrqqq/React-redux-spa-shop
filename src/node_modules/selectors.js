import R from 'ramda'

const getRobotById = (state, id) => R.prop(id, state.robots)

export const getRobots = state => {
    const robots = R.map(id => getRobotById(state, id), state.robotsPage.ids)
    return robots
}